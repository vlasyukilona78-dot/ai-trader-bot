## Calibration triage

For a ready-made operator verdict from comparison JSON, use:
`powershell -ExecutionPolicy Bypass -File .\scripts\observation\triage_calibration_result.ps1 -ComparisonJson <comparison.json>`

See `scripts/observation/RUNBOOK_triage.md` for the full flow and exit code meanings.
